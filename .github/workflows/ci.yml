name: CI

on:
  push:
    branches: [main, feat/cron-subagent]
  pull_request:
    branches: [main, feat/cron-subagent]

permissions:
  contents: read

jobs:
  zig:
    uses: nullclaw/nullbuilder/.github/workflows/zig-ci.yml@v1
    permissions:
      contents: read
    with:
      binary_name: nullclaw
      artifact_prefix: nullclaw
